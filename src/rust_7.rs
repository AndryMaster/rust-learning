use rand::Rng;

use std::{cmp::Ordering, clone};
// use std::cmp::Ordering;
// use std::io;

use std::io::{self, Write};
// use std::io;
// use std::io::Write;

use std::collections::*;
// use std::collections::HashMap;
// use std::collections::hash_map::HashMap;

pub fn f1() {
    let secret_number = rand::rng().random_range(1..=100);
}
